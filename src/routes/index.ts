/**
 * Route generator — takes an introspected collection list, applies config,
 * and registers Fastify routes for each surviving collection.
 *
 * Implementation coming in a subsequent PR.
 */
export {};
